# netlify.toml
[build]
  # Only install dependencies during build; do NOT run streamlit here
  command = "pip install -r requirements.txt"
  publish = "."

[context.production.environment]
  STREAMLIT_SERVER_HEADLESS = "true"
