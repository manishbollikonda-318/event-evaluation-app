# netlify.toml
[build]
  command = "pip install -r requirements.txt && streamlit run app.py --server.port $PORT"
  publish = "."

[context.production.environment]
  STREAMLIT_SERVER_HEADLESS = "true"
